# คู่มือติดตั้งและเผยแพร่เว็บ CPRAM Fleet (ทำตามทีละขั้น)

ใช้เวลาประมาณ 20-30 นาที ไม่ต้องเขียนโค้ดเพิ่ม แค่ตั้งค่าและกดตามขั้นตอน
ทุกขั้นตอนต้องทำโดยเจ้าของระบบ/บริษัท (ผมช่วยสั่งการหรือดำเนินการแทนจากในแชทนี้ไม่ได้ เพราะเป็นการสร้างบัญชีและใส่ข้อมูลการเงิน/ความเป็นเจ้าของของบริษัทคุณเอง) แต่ถามได้ทุกขั้นตอนถ้าติดปัญหา ส่ง error message มาให้ดูได้เลย

---

## ขั้นที่ 1: สร้างฐานข้อมูล (Supabase) — ฟรี

1. ไปที่ https://supabase.com แล้วสมัครสมาชิก (ใช้ GitHub หรืออีเมลก็ได้)
2. กด **New Project** ตั้งชื่อโปรเจกต์ เช่น `cpram-fleet` เลือก region **Southeast Asia (Singapore)** แล้วตั้งรหัสผ่านฐานข้อมูล (เก็บไว้ให้ดี)
3. รอสักครู่จนโปรเจกต์พร้อมใช้งาน
4. ไปที่เมนู **SQL Editor** (แถบซ้าย) → **New query**
5. เปิดไฟล์ `supabase/schema.sql` ที่แนบมาด้วย คัดลอกทั้งหมด แล้ววางในช่อง SQL Editor
6. กด **Run** — จะสร้างตารางรถ / ประวัติซ่อม / พนักงานขับรถ พร้อมข้อมูลตัวอย่างให้อัตโนมัติ
7. ไปที่ **Project Settings > API** คัดลอกค่า 2 ตัวนี้เก็บไว้:
   - **Project URL** (หน้าตาแบบ `https://xxxxx.supabase.co`)
   - **anon public key** (กุญแจยาวๆ)

## ขั้นที่ 2: ตั้งค่าการเข้าสู่ระบบ (ทางเลือก แต่แนะนำ)

โดยค่าเริ่มต้น Supabase จะส่งอีเมลยืนยันเมื่อพนักงานสมัครสมาชิก ถ้าต้องการให้พนักงานเข้าใช้งานได้ทันทีโดยไม่ต้องรอยืนยันอีเมล:

1. ไปที่ **Authentication > Providers > Email**
2. ปิดตัวเลือก **Confirm email**
3. กด Save

(ทำภายหลังได้ ไม่จำเป็นต้องทำตอนนี้)

## ขั้นที่ 3: อัปโหลดโค้ดขึ้น GitHub

1. สร้างบัญชี https://github.com (ถ้ายังไม่มี)
2. สร้าง repository ใหม่ ตั้งชื่อ เช่น `cpram-fleet-web`
3. อัปโหลดไฟล์ทั้งหมดในโฟลเดอร์นี้เข้า repository (ลาก-วางไฟล์ผ่านหน้าเว็บ GitHub ได้เลย ไม่ต้องใช้คำสั่ง git ก็ได้ — กด "Add file > Upload files" แล้วลากทั้งโฟลเดอร์ที่แตกไฟล์ zip แล้วเข้าไป)

## ขั้นที่ 4: เผยแพร่เว็บด้วย Vercel — ฟรี

1. ไปที่ https://vercel.com สมัครสมาชิกด้วยบัญชี GitHub เดียวกัน
2. กด **Add New > Project**
3. เลือก repository `cpram-fleet-web` ที่เพิ่งอัปโหลด
4. ก่อนกด Deploy ให้เปิดส่วน **Environment Variables** แล้วเพิ่ม 2 ค่า:
   - `NEXT_PUBLIC_SUPABASE_URL` = Project URL จากขั้นที่ 1
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` = anon public key จากขั้นที่ 1
5. กด **Deploy** รอประมาณ 1-2 นาที
6. เสร็จแล้วจะได้ลิงก์เว็บไซต์ เช่น `https://cpram-fleet-web.vercel.app` — ส่งลิงก์นี้ให้พนักงานได้เลย

## ขั้นที่ 5: ให้พนักงานเข้าใช้งาน

พนักงานแต่ละคนเปิดลิงก์เว็บไซต์ แล้วกด **สมัครใช้งานใหม่** กรอกอีเมลและตั้งรหัสผ่านเอง ทุกคนที่ล็อกอินแล้วจะเห็นข้อมูลชุดเดียวกันและแก้ไขได้เท่ากันหมด

ถ้าต้องการปิดไม่ให้คนนอกสมัครเองได้ในอนาคต ไปที่ Supabase > Authentication > Providers > Email แล้วปิด **Allow new users to sign up** จากนั้นสร้างบัญชีให้พนักงานเองผ่านเมนู **Authentication > Users > Add user**

---

## ภายหลังอยากต่อโดเมนบริษัทเอง (เช่น fleet.cpram.co.th)

ไปที่โปรเจกต์ใน Vercel > **Settings > Domains** ใส่โดเมนที่ต้องการ แล้วตั้งค่า DNS ตามที่ Vercel แนะนำ (ปกติคือเพิ่ม CNAME record ที่ผู้ให้บริการโดเมนของบริษัท)

## แก้ไขหน้าตา/ฟีเจอร์เพิ่มเติม

โค้ดหน้าเว็บหลักทั้งหมดอยู่ที่ `components/FleetApp.js` ถ้าต้องการปรับแก้ ส่งกลับมาให้ผมช่วยแก้ไขต่อได้เสมอ — ทำโค้ดเสร็จแล้วอัปโหลดทับไฟล์เดิมใน GitHub, Vercel จะดีพลอยเวอร์ชันใหม่ให้อัตโนมัติ
